---
name: Use bun as package manager
description: Project uses bun, not npm or yarn
type: feedback
---

Use `bun` for all package management in this project.

**Why:** User explicitly noted bun is the package manager.

**How to apply:** `bun add` not `npm install`, `bun run` / `bun <script>` not `npm run`, `bunx expo install` for Expo-managed packages.
